# 실제 값을 넣어 .streamlit/secrets.toml 로 저장할 것(gitignore됨, 커밋 금지).
# 백엔드(team_project)의 .env 에 있는 ADMIN_PASSWORD 와 반드시 같은 값이어야 한다 —
# 대시보드가 이 값을 GET /subscribers 호출 시 X-Admin-Password 헤더로 그대로 전달한다.
admin_password = "change_this_admin_password"
